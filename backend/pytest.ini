[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
addopts = -p no:langsmith_plugin
markers =
    perf: performance budget test (Constitution IV) — opt-in via `pytest -m perf`
