# AI 日报系统 · 产品需求文档（PRD）

- **版本**：v1.2
- **日期**：2026-08-12
- **作者**：产品组
- **状态**：对齐《后端集成接口文档 v1.0》的工程可开工版本；v1.x 信息源/类型由接口枚举固定（x / github / reddit / web 四源 + agent / self_improve / open_source / tools 四类型），高度自定义能力推迟到 v2
- **自包含说明**：本文件自包含，与《后端集成接口文档 v1.0》互为参照。**接口文档为权威契约**：v1.x 范围内若本文件与接口文档冲突，以接口文档为准，并在「十二、冲突裁决与版本说明」记录裁决。v1.1.1 时期规划的自定义信息源管理、自定义标签管理、评分体系（specificity / credibility / tier）、`/generate/run` 手动触发、`/daily/{date}` 历史日报、`/search` 检索、`read_state` 独立接口等能力，**接口文档 v1.0 未提供对应接口的，统一推迟到 v2**，本期 PRD 仅保留接口文档已覆盖的能力。

---

## AI日报系统 产品定义

### 一、核心定位与目标

- **产品定位**：高度自定义的私人资讯聚合工具，先个人本地部署使用，成熟后开源至GitHub
- **核心价值**：解决多平台信息源分散痛点，过滤低价值冗余内容，输出精简统一的趣味化解读，摆脱算法投喂，打造专属每日信息入口
- **使用方式**：PC本地网页端浏览，每日自动生成，单次深度阅读时长约30分钟
- **开源特性**：内置默认信息源、领域标签与过滤规则，开箱即用；同时支持全维度自定义配置，满足个性化信息需求

---

### 二、内容生产全链路规则

#### 1. 信息源体系

> v1.2 对齐说明：接口文档 v1.0 §1.1 / §4.3 / §7 明确，本期信息源为**固定 4 源**：`x` / `github` / `reddit` / `web`（全网聚合），不支持用户新增/删除信息源。原 v1.1.1 设计的"精准账号 / 关键词泛抓"管理面板、`kind` 字段、`credibility` / `tier` 可信度分级等能力推迟到 v2（详见 §十一）。本节描述以接口文档为准。

- **采集管线**：X / GitHub / Reddit / 全网聚合 四个信息源，由后端采集管线统一抓取（接口文档 §1.1）
- **本期固定源**（接口文档 §4.3 / §7）：
  - `x` — X (Twitter)，"前沿哨兵，偶尔发疯但信息最快"
  - `github` — GitHub，"仓库动态、新项目、趋势榜单"
  - `reddit` — Reddit，"社区热帖与高赞讨论，一手开发者情报"（2026-08-12 起原 `blogs` 枚举更名 `reddit`，历史数据按接口文档 §7 迁移映射）
  - `web` — 全网聚合，"搜索引擎 + RSS 兜底，不放过漏网之鱼"
- **管理方式**（v1.x）：信息源**不可增删**；用户可在设置面板内通过开关控制每个源是否参与采集（`Settings.sources`），关闭后下一期刊期生效（接口文档 §4.4 / §5.6 / §8）
- **可信度分级**：v1.x 接口文档未提供 credibility/tier 字段，统一打分推迟到 v2。本期排序依赖 LLM 摘要质量 + 用户偏好过滤，不引入数值化可信度系数

#### 2. 过滤与去重规则

> v1.2 对齐说明：接口文档 v1.0 §4.4 `filtersApplied` + §5.6 偏好保存 + §8 与采集管线协作，明确本期过滤逻辑为**按用户偏好（sources / types 开关）过滤**；强制黑名单、全局去重、24h 时效窗口、`daily_count` 截取、`specificity` 多标签归类等复杂规则属于后端管线内部实现，本期 PRD 仅约束用户可见行为。

- **本期过滤范围**（接口文档 §4.4 / §5.6）：用户偏好中的 `sources` 开关 + `types` 开关决定下一期刊期的采集范围；偏好保存后**下一期刊期生效**，当期不回溯（接口文档 §8）
- **LLM 摘要**（接口文档 §1.1 / §1.2）：每条采集内容经 LLM 生成导语 `lede` / 一句话总结 `summary` / 正文 `body[]` / 要点 `points[]`，失败重试 2 次仍失败标记刊期 `failed`
- **刊期状态机**（接口文档 §4.2 / §8）：`generating → ready / failed`；`ready` 后按 `dailyPush` 配置触发推送（默认 08:00）
- **空态行为**（接口文档 §5.2 / §6）：筛选无结果时 `items: []`，前端展示「今天的货架是空的」
- **强制黑名单 / 全局去重 / 24h 时效窗口 / daily_count 截取**：属后端管线内部规则，v1.x 不暴露给用户配置；v1.1.1 设计的细化规则推迟到 v2 配置项开放后落地

#### 3. 权重与排序规则

> v1.2 对齐说明：接口文档 v1.0 §4.1 Article 字段中**没有 `score` 字段**，也未提供 `specificity` / `credibility` / `tier` 等打分维度。本期排序与截取逻辑由后端管线内部决定，前端只接收 `ArticleListItem[]` 渲染。v1.1.1 设计的"综合得分公式 + 多标签 specificity 归类 + 三档权重"体系整体推迟到 v2（详见 §十一）。

- **本期排序**（接口文档 §4.1 / §5.1 / §5.2）：后端管线决定列表顺序，前端按返回的 `items[]` 顺序渲染；不暴露打分逻辑给前端
- **本期截取**：v1.x 接口文档未提供 `daily_count` 配置，每日刊期条目数由后端管线决定（接口文档 §5.1 返回 `articleCount`）；用户可配置的"每日保留条数"推迟到 v2
- **本期分组**（接口文档 §5.1 `summary.byType` / `bySource`）：前端可基于 `summary` 渲染数量徽标，但条目列表本身由后端顺序决定，不做客户端二次分组排序

#### 4. 内容分类组织

> v1.2 对齐说明：接口文档 v1.0 §4.3 Type 元数据为**固定 4 类**：`agent` / `self_improve` / `open_source` / `tools`，本期不支持用户自定义标签。用户只能通过开关控制每个类型是否参与采集（`Settings.types`）。原 v1.1.1 设计的"自定义标签 + 拖拽排序 + `other` 兜底"推迟到 v2。

- **本期固定类型**（接口文档 §4.3 / §7）：
  - `agent` — Agent / 智能体
  - `self_improve` — 持续学习 / 自我进化（短名「持续学习」）
  - `open_source` — 开源项目（短名「开源」）
  - `tools` — 工具与效率（短名「工具效率」）
- **筛选 chips 由 `/meta` 接口驱动**（接口文档 §5.4）：正式版筛选 chips 与设置面板开关列表均由 `GET /meta` 返回，**前端不硬编码**，新增信息源无需前端发版（接口文档 §5.4 末段明确）
- **类型 / 来源双维筛选**（接口文档 §5.2）：`type` 与 `src` 参数可组合，原型「Reddit + Agent」场景通过 `GET /articles?type=agent&src=reddit` 实现
- **`other` 兜底标签**：v1.x 接口文档未提供 `other` 类型，本期不做兜底归类；推迟到 v2 自定义标签体系上线后落地

#### 5. 内容加工标准

> v1.2 对齐说明：接口文档 v1.0 §1.1 / §4.1 明确 LLM 加工产出 `lede` / `summary` / `body[]` / `quote` / `points[]` 五类文本字段。**接口文档未提供 `style_mode` 配置项**，也**未提供按领域生成的核心结论 `conclusions` 字段**。本期风格档位（严谨/平衡/活泼）与核心结论能力整体推迟到 v2。

- **LLM 加工字段**（接口文档 §1.1 / §4.1）：每条采集内容由 LLM 生成 5 类文本：
  - `lede` — 导语（阅读器首段）
  - `summary` — 一句话总结（摘要框）
  - `body` — 正文段落（`string[]`）
  - `quote` — 引用块，可为空
  - `points` — 要点列表（`string[]`）
- **风格档位**：v1.x 接口文档未提供配置，本期由后端 prompt 固定为"正经干货 + 口语化俏皮"的单一风格（接口文档 §1.1「每天 5 分钟，跟上 AI 的脑回路」基调）；严谨/平衡/活泼三档调节推迟到 v2
- **核心结论**：v1.x 接口文档 §5.1 响应中**无 `conclusions` 字段**，本期不做"按领域核心结论"展示；该能力推迟到 v2
- **`readingMinutes`**（接口文档 §4.1）：后端为每条计算预计阅读分钟数，前端用于展示

---

### 三、页面结构与核心功能

> v1.2 对齐说明：接口文档 v1.0 §3 接口总览 + §6 前端接入对照表，明确本期页面交互的核心入口为 9 个接口。原 v1.1.1 设计的"历史日历 / 关键词检索 / 已读状态持久化 / 信息源管理面板"等功能，**接口文档 v1.0 未提供对应接口**（无 `/daily/{date}` / `/search` / `/read-state` / CRUD `/sources`），统一推迟到 v2。本期页面结构对齐接口文档 §6 的交互点。

#### 1. 整体布局：索引+阅读器单页架构

- 全产品采用**左侧索引 + 右侧阅读器**的单页设计，所有功能与内容均在同一页面内完成承载与切换，无额外独立页面层级（与接口文档 §6 "前端原型 `v1 ai 日报.html`"一致）
- 左侧索引区：固定展示当日刊期的条目索引、数量徽标、按类型/来源筛选 chips
- 右侧阅读器区：根据左侧索引的选择，动态切换展示对应内容视图（日报全文、资讯详情、设置面板）

#### 2. 左侧索引区核心模块（接口文档 §5.1 / §5.2 / §5.4）

- **报头**：展示当日刊期日期、版次（`edition`）、状态（接口文档 §4.2 / §5.1）
- **数量徽标**：基于 `summary.byType` 与 `summary.bySource` 渲染（接口文档 §5.1）
- **类型筛选 chips**：由 `GET /meta` 返回的 `types[]` 驱动，切换即调用 `GET /articles?type=`（接口文档 §5.2 / §5.4）
- **来源筛选 chips**：由 `GET /meta` 返回的 `sources[]` 驱动，可与类型筛选组合（接口文档 §5.2 / §5.4）
- **条目索引列表**：基于 `GET /daily/today` 返回的 `articles[]`（含 `id` / `title` / `excerpt` / `type` / `src` / `time` / `readingMinutes`），点击切换右侧详情

> v1.2 说明：原 v1.1.1 设计的"历史日历 / 关键词检索"模块推迟到 v2（接口文档 v1.0 未提供 `/daily/{date}` / `/search` / `/daily/calendar`）。

#### 3. 右侧阅读器核心视图（接口文档 §5.1 / §5.3 / §5.5 / §5.6 / §5.8）

- **日报全文视图**：基于 `GET /daily/today` 渲染报头 + 数量徽标 + 条目索引；点击条目进入详情
- **资讯详情视图**（接口文档 §5.3）：基于 `GET /articles/{id}` 渲染完整 Article（§4.1 全字段）
  - 必备操作：「阅读原文」直接使用 `article.sourceUrl` 在新浏览器标签页打开（接口文档 §3 末明确"不单独设接口"）
  - 必备操作：「分享这条」调用 `POST /share` 生成分享卡片，返回 `cardUrl`（接口文档 §5.8）
- **设置面板视图**（接口文档 §5.5 / §5.6 / §5.7）：
  - 打开时调用 `GET /settings` 回填当前偏好 + `GET /meta` 渲染开关列表
  - 「保存偏好」调用 `PUT /settings` 全量提交当前开关状态，响应头 `X-Effective-At` 提示生效刊期
  - 「恢复默认」调用 `POST /settings/reset`，回填返回的默认值（全开关开启、推送 08:00）
- **加载态 / 空态 / 错误态**（接口文档 §2.2 / §6）：
  - 刊期 `generating`（2003）→ 骨架屏 + 轮询
  - 刊期未就绪（2002，如 08:00 前访问）→ 「正在翻今天的墙头」加载态
  - 筛选无结果 → 「今天的货架是空的」
  - 错误 toast → 读统一错误结构的 `message`

> v1.2 说明：原 v1.1.1 设计的"已读视觉淡化 / 自动标记已读"推迟到 v2（接口文档 v1.0 §4.1 Article 无 `is_read` 字段，无 `/read-state` 接口）。

#### 4. 核心功能清单（本期 v1.x 范围）

- **今日刊浏览**：报头 + 索引 + 数量徽标，一次 `GET /daily/today` 完成首屏（接口文档 §5.1）
- **双维筛选**：类型 + 来源 chips 可组合筛选（接口文档 §5.2）
- **资讯详情**：完整字段展示，包含 `lede` / `summary` / `body[]` / `quote` / `points[]` / `sourceUrl` / `sourceName` / `readingMinutes` / `publishedAt`（接口文档 §5.3）
- **阅读原文**：直接打开 `sourceUrl`（接口文档 §3）
- **分享卡片**：`POST /share` 返回 `cardUrl`（接口文档 §5.8）
- **用户偏好**：信息源开关 + 类型开关 + 每日推送时间，保存后下一期刊期生效（接口文档 §5.5 / §5.6 / §5.7 / §8）
- **恢复默认**：一键回到全开 + 08:00 推送（接口文档 §5.7）

> v1.2 推迟到 v2 的功能：历史日历、历史日报访问、关键词检索、已读跟踪、自定义信息源/标签管理、风格档位、核心结论、`daily_count` 自定义、`gen_time` 自定义、`src_filter` 永久筛选状态。详见 §十一。

---

### 四、自定义能力

> v1.2 对齐说明：接口文档 v1.0 §4.4 Settings 模型仅含三类配置：`sources` 开关、`types` 开关、`dailyPush`（含 `enabled` 与 `time`）。原 v1.1.1 设计的"信息源 CRUD / 标签 CRUD / daily_count 枚举 / style_mode 三档 / 领域拖拽排序 / src_filter 永久状态"等高级自定义能力，**接口文档 v1.0 未提供对应接口**，统一推迟到 v2。

#### 本期 v1.x 自定义能力（接口文档 §4.4）

1. **信息源开关**：`Settings.sources` 为每个固定源（`x` / `github` / `reddit` / `web`）维护一个布尔开关，关闭后下一期刊期不再采集该源
2. **类型开关**：`Settings.types` 为每个固定类型（`agent` / `self_improve` / `open_source` / `tools`）维护一个布尔开关，关闭后下一期刊期不再采集该类型
3. **每日推送开关 + 时间**：`Settings.dailyPush.enabled` + `Settings.dailyPush.time`（HH:mm，默认 "08:00"）
4. **恢复默认**：`POST /settings/reset` 一键回到全开 + 08:00 推送

#### 生效时机（接口文档 §4.4 / §5.6 / §8）

- 偏好保存后**下一期刊期生成立即生效**，当期不回溯
- 前端提示语「明天的日报将按新口味调配」与该规则一致
- `PUT /settings` 响应头 `X-Effective-At`（如 `20260813`）告知前端生效刊期

#### 推迟到 v2 的自定义能力（详见 §十一）

- 信息源增删（精准账号 / 关键词泛抓 / `kind` 字段 / `credibility` / `tier`）
- 标签增删与权重档位（`weight` / `specificity` / `order`）
- `daily_count` 枚举（5/8/10/12）
- `style_mode` 三档（strict / balanced / lively）
- 领域拖拽排序
- `src_filter` 永久筛选状态

---

### 五、数据契约（产品视角）

> v1.2 对齐说明：本章对齐接口文档 v1.0 §4 数据模型。**接口文档为权威**：字段命名（`camelCase`）、字段类型、必填性、枚举值均以接口文档为准。v1.1.1 时期的 snake_case 字段（`source_url` / `published_at` / `is_read` 等）、`score` / `specificity` / `credibility` / `tier` 等字段不在本期范围内。

#### 1. 资讯条目（Article，接口文档 §4.1）

| 字段 | 类型 | 必填 | 产品行为约束 |
|---|---|---|---|
| `id` | string | 是 | 唯一标识，如 `20260812-0003` |
| `issueId` | string | 是 | 所属刊期，如 `20260812` |
| `type` | enum | 是 | `agent` / `self_improve` / `open_source` / `tools` |
| `src` | enum | 是 | `x` / `github` / `reddit` / `web` |
| `title` | string | 是 | 标题 |
| `excerpt` | string | 是 | 列表摘要（前端 ≤ 2 行截断） |
| `lede` | string | 是 | 导语（阅读器首段） |
| `summary` | string | 是 | 一句话总结（摘要框） |
| `body` | string[] | 是 | 正文段落 |
| `quote` | string / null | 否 | 引用块，可为空 |
| `points` | string[] | 是 | 要点列表 |
| `time` | string | 是 | 收录时间 `HH:mm`（接口文档格式，注意非 `time_label`） |
| `sourceUrl` | string | 是 | 原文链接，「阅读原文」按钮唯一数据源 |
| `sourceName` | string | 是 | 原文站点名（如 `simonwillison.net`） |
| `readingMinutes` | number | 是 | 预计阅读分钟数 |
| `publishedAt` | string | 是 | 原文发布时间，ISO 8601 |

> **列表卡片 ArticleListItem**（接口文档 §5.1）：仅含 `id` / `title` / `excerpt` / `type` / `src` / `time` / `readingMinutes`，长文 `lede` / `body` / `quote` / `points` 仅在详情接口返回，避免列表请求体过大。

> **v1.x 不在 Article 中的字段**（推迟到 v2）：`score` / `is_read` / `specificity` / `credibility` / `tier` / `weight` / `order` / `builtin`。

#### 2. 刊期（DailyIssue，接口文档 §4.2）

| 字段 | 类型 | 产品行为约束 |
|---|---|---|
| `id` | string | 刊期 id，如 `20260812`（YYYYMMDD） |
| `date` | string | `YYYY-MM-DD` |
| `edition` | number | **版次**（第 N 版），v1.2 新增概念，前端报头展示 |
| `status` | enum | `generating` / `ready` / `failed`；前端按此渲染加载态/正常态/错误态 |
| `generatedAt` | string | 生成完成时间 |
| `articleCount` | number | 条目总数 |
| `filtersApplied` | object | 本期实际采集范围 `{ sources: [], types: [] }`，来源用户偏好 |

> **v1.x 不在 DailyIssue 中的字段**（推迟到 v2）：`style_mode` / `count` / `conclusions` / `sections`。本期无"按领域核心结论"与"按领域分组快照"，列表顺序由后端决定。

#### 3. 元数据（Source / Type，接口文档 §4.3 / §5.4）

| 对象 | 字段 | 产品行为约束 |
|---|---|---|
| Source | `key` | `x` / `github` / `reddit` / `web` |
| Source | `name` | 展示名，如 `X (Twitter)` / `Reddit` |
| Source | `short` | 短名，如 `X` / `全网` |
| Source | `icon` | 前端图标 key：`x` / `github` / `reddit` / `globe` |
| Source | `description` | 一句话描述 |
| Type | `key` | `agent` / `self_improve` / `open_source` / `tools` |
| Type | `name` | 展示名，如 `Agent / 智能体` |
| Type | `shortName` | 短名，如 `Agent` |

> **关键**（接口文档 §5.4 末段）：正式版筛选 chips 与设置面板开关列表**由 `/meta` 接口驱动，前端不硬编码**，新增信息源/类型无需前端发版。

#### 4. 用户偏好（Settings，接口文档 §4.4）

```json
{
  "sources": { "x": true, "github": true, "reddit": true, "web": true },
  "types": { "agent": true, "self_improve": true, "open_source": true, "tools": true },
  "dailyPush": { "enabled": true, "time": "08:00" },
  "updatedAt": "2026-08-12T10:30:00+08:00"
}
```

| 字段 | 产品行为约束 |
|---|---|
| `sources` | 4 源开关 map；关闭后下一期刊期不再采集该源 |
| `types` | 4 类型开关 map；关闭后下一期刊期不再采集该类型 |
| `dailyPush.enabled` | 每日推送总开关 |
| `dailyPush.time` | 推送时间 HH:mm，默认 "08:00" |
| `updatedAt` | 最后修改时间，前端用于展示「上次保存于」 |

> **生效时机**（接口文档 §4.4）：保存后**下一期刊生成立即生效**，当期不回溯。

> **v1.x 不在 Settings 中的字段**（推迟到 v2）：`daily_count` / `style_mode` / `gen_time` / `src_filter` / `read_ids`。

#### 5. 分享卡片（Share，接口文档 §5.8）

| 字段 | 产品行为约束 |
|---|---|
| `shareId` | 分享卡片 id，如 `shr_9f2c` |
| `cardUrl` | 分享卡片 URL，前端打开或复制 |
| `articleTitle` | 文章标题，便于分享前预览 |

---

### 六、评分与排序细则

> v1.2 对齐说明：接口文档 v1.0 §4.1 Article 字段中**没有 `score` 字段**，§4 数据模型中也**未提供** `specificity` / `credibility` / `tier` / `weight` 等打分维度。v1.1.1 时期设计的"综合得分公式 + 多标签 specificity 归类 + 三档权重 + 全局排序截取 + 分组展示顺序 + 平台筛选执行顺序"等完整评分体系，**整体推迟到 v2**（详见 §十一）。本期 v1.x 不做产品可见的打分排序约束。

#### 本期 v1.x 排序约定

- **列表顺序**（接口文档 §5.1 / §5.2）：由后端管线内部决定，前端按返回的 `articles[]` / `items[]` 顺序渲染，不做客户端二次排序
- **条目数量**：每日刊期条目数由后端决定，接口返回 `articleCount` 告知前端；用户不可配置
- **筛选**（接口文档 §5.2）：`type` 与 `src` 参数过滤由后端执行，返回 `appliedFilters` 回显
- **数量徽标**（接口文档 §5.1 `summary.byType` / `bySource`）：仅用于展示分桶数量，不驱动排序

#### v2 评分体系（规划中，本期不实现）

v1.1.1 时期的完整评分体系作为 v2 候选设计保留，待接口文档扩展 `score` / `specificity` / `credibility` 等字段后落地：

- 综合得分公式：`final_score = type_weight_coefficient × source_credibility`
- 系数档位：type_weight（high 1.2 / mid 1.0 / low 0.8）× source_credibility（official 1.0 / media 0.85 / community 0.7）
- 多标签命中按 specificity 取最大者归类
- 全局排序取前 N 条（N = `daily_count`）
- 分组顺序按 `types.order`，组内按 `score` 降序
- 平台筛选在截取后执行，不触发补位

详见 §十一 v2 backlog。

---

### 七、默认值与配置基线

> v1.2 对齐说明：本章仅保留接口文档 v1.0 明确的默认值。v1.1.1 时期的 `daily_count` / `style_mode` / `gen_time` / `src_filter` / 内置标签权重表等基线，**接口文档 v1.0 未提供对应字段**，统一推迟到 v2。

#### 本期 v1.x 默认值（接口文档 §4.4 / §5.7）

| 配置项 | 默认值 | 取值范围 | 说明 |
|---|---|---|---|
| `sources.x` | **true** | bool | X 源开关 |
| `sources.github` | **true** | bool | GitHub 源开关 |
| `sources.reddit` | **true** | bool | Reddit 源开关（原 `blogs`，2026-08-12 更名） |
| `sources.web` | **true** | bool | 全网聚合源开关 |
| `types.agent` | **true** | bool | Agent 类型开关 |
| `types.self_improve` | **true** | bool | 持续学习类型开关 |
| `types.open_source` | **true** | bool | 开源类型开关 |
| `types.tools` | **true** | bool | 工具效率类型开关 |
| `dailyPush.enabled` | **true** | bool | 每日推送总开关 |
| `dailyPush.time` | **"08:00"** | HH:mm（24h） | 每日推送时间 |
| 刊期生成时间 | **08:00** | — | 接口文档 §1.1 / §8 明确默认 08:00 按用户偏好生成 |
| 时区 | **Asia/Shanghai (UTC+8)** | — | 接口文档 §2 通用约定 |
| 分页 `page` | **1** | ≥ 1 | 接口文档 §2 |
| 分页 `pageSize` | **20**，最大 **50** | 1-50 | 接口文档 §2 |
| 读接口限流 | **120 次/分钟/IP** | — | 接口文档 §2 |
| 写接口限流 | **30 次/分钟/用户** | — | 接口文档 §2 |

> **恢复默认**（接口文档 §5.7）：`POST /settings/reset` 返回上述全开 + 08:00 推送的默认 Settings 对象。

#### 推迟到 v2 的默认值（详见 §十一）

- `daily_count`（5/8/10/12，默认 10）
- `style_mode`（strict / balanced / lively，默认 balanced）
- `gen_time`（与 `dailyPush.time` 在 v1.x 复用同一字段，v2 可能拆分）
- `src_filter`（永久筛选状态）
- 内置标签权重表（`agent: high` / `self_improve: mid` / `open_source: mid` / `tools: mid` / `other: low`）

---

### 八、异常态产品行为

> v1.2 对齐说明：本章错误场景与错误码完全对齐接口文档 v1.0 §2.2 错误码表 + §5 各接口错误场景 + §6 前端接入对照表。v1.1.1 时期的自定义错误码（50301 / 40401 / 40901 等）已被接口文档的业务码（1001-9002）取代。

#### 1. 错误码对照表（接口文档 §2.2）

| HTTP | 业务码 | 含义 | 前端表现 |
|---|---|---|---|
| 400 | **1001** | 缺少必填参数 | 表单级提示 |
| 400 | **1002** | 枚举值非法（type / src 等） | 筛选 chips 提示 + 不发起请求 |
| 401 | **1003** | 未认证或 token 失效 | 跳登录（写接口场景） |
| 403 | **1004** | 无权限 | 提示无权限 |
| 404 | **2001** | 文章不存在 | 详情视图显示「内容不存在」 |
| 404 | **2002** | 刊期不存在或未生成完成 | 今日刊视图显示「正在翻今天的墙头」加载态 |
| 409 | **2003** | 刊期正在生成中，请稍后重试 | 保留骨架屏 + 轮询 |
| 422 | **1005** | 请求体校验失败（如 `dailyPush.time` 非法） | 表单级提示，注明字段 |
| 429 | **1006** | 触发限流 | toast「操作太频繁，稍后再试」 |
| 500 | **9001** | 服务内部错误 | 全局错误态 + 重试按钮 |
| 503 | **9002** | 采集 / 摘要管线繁忙 | 全局错误态 + 重试按钮 |

#### 2. 统一错误结构（接口文档 §2.1）

```json
{
  "code": 2002,
  "message": "刊期不存在或尚未生成",
  "requestId": "req_8f3a1c..."
}
```

- 前端读 `message` 展示 toast
- `requestId` 用于排障，前端可在日志中记录

#### 3. 关键场景的前端表现（接口文档 §5 / §6 / §8）

| 场景 | 接口 | 业务码 | 前端表现 |
|---|---|---|---|
| 首页加载，刊期就绪 | `GET /daily/today` | 200 | 渲染报头 + 索引 + 数量徽标 |
| 首页加载，刊期生成中 | `GET /daily/today` | **2003** | 骨架屏 + 轮询 |
| 首页加载，刊期未生成（08:00 前访问） | `GET /daily/today` | **2002** | 「正在翻今天的墙头」加载态 |
| 组合筛选无结果 | `GET /articles?src=reddit&type=agent` | 200，`items: []` | 「今天的货架是空的」空态 |
| 非法枚举（如 `src=wechat`） | `GET /articles?src=wechat` | **1002** | 筛选 chips 提示 |
| 文章不存在 | `GET /articles/{id}` | **2001** | 「内容不存在」 |
| 保存偏好校验失败（如 `dailyPush.time=25:00`） | `PUT /settings` | **1005** | 表单级提示 |
| 未认证访问写接口 | `GET /settings`（无 token） | **1003** | 跳登录 |
| 文章不存在（分享场景） | `POST /share` | **2001** | toast「文章不存在」 |

#### 4. 管线失败重试（接口文档 §8）

- LLM 摘要失败自动重试 **2 次**，仍失败标记刊期 `status: failed`
- 刊期 `failed` 时前端收到 `2002`，与"未生成"行为一致
- 偏好保存异步同步给采集调度器，**下一期刊期生效**，当期不回溯

#### 5. 健康检查（接口文档 §5.9）

- `GET /healthz` 返回 `{ status, version, pipeline: { collector, summarizer } }`
- 前端可用于联调与监控，不向终端用户展示

---

### 九、非功能性需求

> v1.2 对齐说明：本章性能/可用性/安全约束对齐接口文档 v1.0 §2 通用约定。原 v1.1.1 的 `/search` 性能指标、`POST /generate/run` 幂等性、配置变更回溯改写约束等条目，对应能力本期无接口，统一推迟到 v2。

#### 1. 通用约定（接口文档 §2）

| 约定项 | 规则 |
|---|---|
| Base URL | `https://{host}/api/v1`（联调环境由后端提供） |
| 协议 / 编码 | HTTPS；JSON；UTF-8 |
| 认证 | **读接口匿名可用**；**写接口需 `Authorization: Bearer <token>`** |
| 请求 ID | 客户端可传 `X-Request-Id` 头，响应同值返回，便于排障 |
| 时间 | ISO 8601，时区 `Asia/Shanghai`（UTC+8） |
| 分页 | `page` 从 1 开始；`pageSize` 默认 20，最大 50 |
| 枚举 | 见 §4 数据模型；非法枚举返回业务码 `1002` |
| 幂等 | `PUT /settings` 全量覆盖，重复提交安全 |
| 限流 | 读 **120 次/分钟/IP**；写 **30 次/分钟/用户** |

#### 2. 性能

| 指标 | 目标 | 测量场景 |
|---|---|---|
| `GET /daily/today` 响应时间 | P95 ≤ 500ms（本地部署，不含采集） | 单用户今日刊首屏 |
| `GET /articles` 响应时间 | P95 ≤ 300ms | 列表 + 筛选 + 分页 |
| `GET /articles/{id}` 响应时间 | P95 ≤ 200ms | 单条详情 |
| 刊期生成耗时 | 后端管线内部指标，前端不感知 | 默认 08:00 触发 |
| 前端首屏可交互 | ≤ 1.5s（本地） | 今日刊首屏 |

> v1.x 不涉及 `/search` 性能指标（接口本期未提供）。

#### 3. 可用性

- 本地单用户部署：单机可用性即可，不要求 7×24 在线；进程重启后能恢复到上次状态
- 刊期生成任务幂等：同一刊期 `id` 重复触发不会生成两份（接口文档 §4.2 刊期 id 唯一）
- 刊期生成失败可重试：LLM 摘要失败自动重试 2 次，仍失败标记 `failed`（接口文档 §8）
- 偏好保存异步生效：下一期刊期生效，当期不回溯（接口文档 §4.4 / §8）
- `PUT /settings` 全量覆盖，重复提交安全（接口文档 §2 幂等约定）

#### 4. 安全与隐私

- **读接口匿名可用，写接口需 `Authorization: Bearer <token>`**（接口文档 §2）
- 用户偏好（信息源开关、类型开关、推送时间）属于私人数据，**不可外发**；开源版需明确告知部署者数据归属
- 信息源抓取需遵守目标平台 robots / ToS；抓取频率需做限速，避免被源站封禁
- 原文链接 `sourceUrl` 仅做跳转，不缓存原文全文（避免版权风险）
- 错误信息不得泄露内部栈、SQL、密钥等敏感数据（接口文档 §2.1 错误结构仅含 `code` / `message` / `requestId`）

#### 5. 国际化与本地化

- v1.x **仅支持简体中文**；时间格式统一 ISO 8601 + 本地时区展示（接口文档 §2）
- 信息源/类型展示名以 `/meta` 返回为准（含中英混合，如 `X (Twitter)` / `Agent / 智能体`）
- 时间展示为 `HH:mm`（接口文档 §4.1 `time` 字段）与 `YYYY-MM-DD`
- v2 视社区反馈再考虑多语言

#### 6. 可维护性与开源准备

- 元数据（4 源 + 4 类型）由 `/meta` 接口驱动，**前端不硬编码**（接口文档 §5.4）
- 数据库 schema 需有版本化迁移脚本
- README 需覆盖：本地启动步骤、配置项说明、9 个接口的调用示例、日志位置
- 关键算法（LLM 摘要 prompt、过滤、采集调度）需有单元测试覆盖

#### 7. 可观测性

- 每个请求支持 `X-Request-Id` 头透传，便于排障（接口文档 §2）
- `GET /healthz` 暴露 `pipeline.collector` / `pipeline.summarizer` 健康状态（接口文档 §5.9）
- 刊期 `status` 字段（`generating` / `ready` / `failed`）便于监控生成质量
- LLM 摘要/采集失败需记录结构化日志，便于成本与稳定性监控

---

### 十、验收标准 UAT

> v1.2 对齐说明：本章按接口文档 v1.0 §9 联调自测清单 + §3 接口总览的 9 个接口重组。原 v1.1.1 的 UAT 模块 3（历史日历）、模块 4（检索）、模块 5（信息源管理）、模块 6（标签管理）涉及的能力本期无接口，对应验收项推迟到 v2。

#### 模块 1 · 今日刊首屏（接口文档 §5.1 / §9）

- [ ] `GET /daily/today` 返回 200，`issue.status = ready`
- [ ] 报头展示日期、版次（`edition`）、状态徽标
- [ ] 数量徽标按 `summary.byType` / `summary.bySource` 渲染
- [ ] 索引列表渲染 `articles[]` 的 7 字段（id / title / excerpt / type / src / time / readingMinutes）
- [ ] 刊期生成中（2003）→ 骨架屏 + 轮询
- [ ] 刊期未生成（2002，08:00 前访问）→ 「正在翻今天的墙头」加载态
- [ ] 刊期失败（2002）→ 与未生成行为一致

#### 模块 2 · 列表筛选与分页（接口文档 §5.2 / §9）

- [ ] 类型筛选 chips 切换调用 `GET /articles?type=`
- [ ] 来源筛选 chips 切换调用 `GET /articles?src=`
- [ ] 类型 + 来源组合筛选可工作（`?src=reddit&type=agent`）
- [ ] 响应 `appliedFilters` 回显当前筛选条件
- [ ] 筛选无结果（`items: []`）→ 「今天的货架是空的」空态
- [ ] 非法枚举（`src=wechat`）→ 400 / `1002`
- [ ] 分页参数 `page` / `pageSize` 工作正常，`pageSize` 最大 50

#### 模块 3 · 资讯详情（接口文档 §5.3 / §9）

- [ ] 点击索引条目调用 `GET /articles/{id}`，切换右侧阅读器
- [ ] 详情渲染完整 Article：`lede` / `summary` / `body[]` / `quote` / `points[]` / `sourceUrl` / `sourceName` / `readingMinutes` / `publishedAt`
- [ ] 「阅读原文」直接打开 `sourceUrl`（新标签页）
- [ ] 文章不存在（404 / `2001`）→ 「内容不存在」

#### 模块 4 · 元数据驱动（接口文档 §5.4）

- [ ] `GET /meta` 返回 4 个 sources + 4 个 types
- [ ] 类型筛选 chips 由 `/meta` 的 `types[]` 渲染，**前端不硬编码**
- [ ] 来源筛选 chips 由 `/meta` 的 `sources[]` 渲染，**前端不硬编码**
- [ ] 设置面板开关列表由 `/meta` 渲染
- [ ] 后端新增信息源无需前端发版即可生效

#### 模块 5 · 用户偏好（接口文档 §5.5 / §5.6 / §5.7）

- [ ] 打开设置面板调用 `GET /settings`（带 Bearer token）回填当前偏好
- [ ] 「保存偏好」调用 `PUT /settings` 全量提交 sources + types + dailyPush
- [ ] 响应头 `X-Effective-At`（如 `20260813`）告知生效刊期
- [ ] 校验失败（`dailyPush.time` 非法）→ 422 / `1005`
- [ ] 未认证访问 → 401 / `1003`
- [ ] 「恢复默认」调用 `POST /settings/reset`，回填全开 + 08:00 推送

#### 模块 6 · 分享卡片（接口文档 §5.8）

- [ ] 「分享这条」调用 `POST /share` 提交 `articleId`
- [ ] 返回 `shareId` / `cardUrl` / `articleTitle`
- [ ] 文章不存在 → 404 / `2001`
- [ ] 未认证 → 401 / `1003`

#### 模块 7 · 健康检查（接口文档 §5.9）

- [ ] `GET /healthz` 返回 `{ status: "ok", version, pipeline: { collector, summarizer } }`
- [ ] 联调与监控可用

#### 模块 8 · 异常态与限流

- [ ] 缺少必填参数 → 400 / `1001`
- [ ] 非法枚举 → 400 / `1002`
- [ ] 校验失败 → 422 / `1005`
- [ ] 触发限流 → 429 / `1006`（读 120/min/IP，写 30/min/用户）
- [ ] 服务内部错误 → 500 / `9001`
- [ ] 管线繁忙 → 503 / `9002`
- [ ] 错误响应仅含 `code` / `message` / `requestId`，不泄露内部栈

#### 模块 9 · 非功能性

- [ ] 读接口匿名可用，写接口需 Bearer token
- [ ] `X-Request-Id` 头透传工作正常
- [ ] 时间统一 ISO 8601，时区 Asia/Shanghai
- [ ] `PUT /settings` 全量覆盖幂等，重复提交安全
- [ ] `GET /daily/today` P95 ≤ 500ms
- [ ] `GET /articles/{id}` P95 ≤ 200ms
- [ ] 元数据由 `/meta` 驱动，无硬编码

---

### 十一、范围外（v2 backlog）

> v1.2 对齐说明：原 v1.1.1 PRD 中规划但**接口文档 v1.0 未提供对应接口**的能力，统一列入 v2 backlog。重新评估条件为：接口文档扩展对应接口后即可启动。

#### A. 本期因接口缺失推迟的能力（v1.1.1 已设计，v1.2 推迟）

| 能力 | 缺失的接口/字段 | 重新评估条件 |
|---|---|---|
| 历史日历 + 历史日报访问 | 无 `GET /daily/{date}` / `GET /daily/calendar` | 接口文档扩展历史访问接口 |
| 关键词检索 | 无 `GET /search` | 接口文档扩展检索接口 |
| 已读跟踪（自动标记 + 视觉淡化） | Article 无 `is_read` 字段，无 `/read-state` 接口 | 接口文档扩展已读接口 |
| 自定义信息源管理（增删/精准账号/关键词） | 无 CRUD `/sources`，无 `kind` / `credibility` / `tier` 字段 | 接口文档扩展信息源管理接口 |
| 自定义领域标签管理 | 无 CRUD `/types`，无 `weight` / `specificity` / `order` 字段 | 接口文档扩展标签管理接口 |
| `daily_count` 自定义（5/8/10/12） | Settings 无 `daily_count` 字段 | 接口文档扩展 Settings |
| `style_mode` 三档（strict/balanced/lively） | Settings 无 `style_mode` 字段，DailyIssue 无 `style_mode` | 接口文档扩展风格档位 |
| 当日核心结论（按领域生成） | DailyIssue 无 `conclusions` 字段 | 接口文档扩展结论字段 |
| 按领域分组快照展示 | DailyIssue 无 `sections` 字段 | 接口文档扩展分组字段 |
| 综合评分体系（`score` + 公式） | Article 无 `score` 字段 | 接口文档扩展评分字段 |
| `src_filter` 永久筛选状态 | Settings 无 `src_filter` 字段 | 接口文档扩展筛选持久化 |
| 手动「立即生成」 | 无 `POST /generate/run` | 接口文档扩展手动触发接口 |
| 当日日报重算（配置变更联动） | 无重算接口，且本期偏好下一期生效 | 接口文档扩展即时生效语义 |

#### B. 长期不做的能力（与产品哲学/定位冲突）

| 能力 | 不做原因 | 重新评估条件 |
|---|---|---|
| 多用户 / 账号体系 | 本地单用户起步，开源版仍是自部署单租户 | 社区出现明确的多租户部署诉求 |
| 移动端 / 原生 App | 定位为 PC 本地网页端，单次深度阅读 30 分钟场景 | 移动端访问占比 > 20% |
| 推送通知（邮件 / IM） | 本地网页端每日固定时间访问即可 | 用户反馈主动触达是刚需 |
| 收藏 / 标注 / 笔记 | 与「精简统一」定位冲突，且超出 MVP | 多用户在 issue 中提出 |
| 自动新增领域标签 | 明确由用户手动管理，系统不自动新增 | 自动归类准确率 ≥ 95% 且用户接受 |
| 多语言界面 | v1.x 仅简体中文 | 社区贡献者提交 i18n PR |
| 跨设备同步 | 本地部署，无云端 | 云端版本进入规划时 |
| 热度图 / 阅读统计仪表盘 | 与核心价值（精简入口）无关 | 数据沉淀足够且用户主动要求 |
| 算法个性化推荐 | 产品反向定位「摆脱算法投喂」 | **永不做**（与产品哲学冲突） |
| RSS / Atom 输出 | 与「单页深度阅读」入口定位不同 | 用户明确希望接入第三方阅读器 |
| 原文全文缓存与搜索 | 版权风险，且 `sourceUrl` 跳转已够用 | 法律侧确认可行且社区强烈要求 |

---

### 十二、冲突裁决与版本说明

> 本节记录对《后端集成接口文档 v1.0》的裁决决策。**v1.2 起，接口文档为本期范围的权威契约**：v1.x 范围内若 PRD 历史版本与接口文档冲突，以接口文档为准。

#### 决策 1 ~ 4（v1.1 时期，已固化）

历史决策 1-4 涉及的 `style_mode` 默认值、`read_state` 独立、检索范围、`daily_count` 默认值，**所对应的字段与接口在接口文档 v1.0 中均不存在**。v1.2 起这些决策自动转入 v2 backlog（详见 §十一），等接口文档扩展对应字段后再行生效。本节保留决策编号用于历史追溯。

#### 决策 5（v1.2 新增）：本期范围以接口文档 v1.0 为权威，PRD v1.1.1 自定义能力推迟到 v2

- **背景**：v1.1.1 时期 PRD 设计了完整的"高度自定义"产品形态（自定义信息源/标签 CRUD、daily_count/style_mode 配置、评分体系、检索、历史、已读等）。但接口文档 v1.0 实际仅提供 9 个接口（§3），覆盖固定 4 源 + 4 类型 + 开关式偏好 + 分享卡片。
- **裁决**：v1.x 范围**严格对齐接口文档 v1.0**。PRD v1.1.1 中规划但接口文档未提供的能力，统一推迟到 v2 backlog。
- **理由**：
  1. **接口文档是工程可开工的权威契约**，PRD 不能凭空定义未对齐的接口
  2. **MVP 优先**：固定 4 源 + 4 类型已能验证"采集 → LLM 摘要 → 刊期 → 浏览 → 分享 → 偏好"端到端链路
  3. **避免范围蔓延**：v1.x 聚焦核心阅读体验，自定义能力在 v2 由社区反馈驱动优先级
- **影响**：PRD §二 / §三 / §四 / §五 / §六 / §七 / §八 / §十 已全面修订，未覆盖能力列入 §十一 v2 backlog

#### 决策 6（v1.2 新增）：字段命名以接口文档 camelCase 为准，前端集成层做 kebab-case 映射

- **背景**：接口文档 v1.0 §7 明确"前端原型使用 kebab-case 键，接口统一 snake_case"。但实际接口文档 §4 数据模型中的字段全部为 **camelCase**（`issueId` / `sourceUrl` / `publishedAt` / `readingMinutes` / `generatedAt` / `articleCount` / `filtersApplied` / `shortName` / `dailyPush` / `updatedAt`）。
- **裁决**：以接口文档 §4 实际字段命名为准（camelCase）。接口文档 §7 描述的"snake_case"应理解为枚举值的多单词分隔（如 `self_improve` / `open_source`），与字段命名无关。
- **理由**：字段定义（§4）是工程直接消费的契约，优先级高于描述性章节（§7）。
- **影响**：PRD §五数据契约表全面采用 camelCase 字段名

#### 决策 7（v1.2 新增）：信息源 `blogs` → `reddit` 更名生效

- **背景**：接口文档 v1.0 §7 明确"2026-08-12 起，原「博客」源已更名"，`blogs` 枚举统一映射为 `reddit`，历史数据需迁移。
- **裁决**：本期固定 4 源为 `x` / `github` / `reddit` / `web`，不再保留 `blogs`。
- **影响**：PRD §二.1 信息源体系、§七默认值基线、§五元数据表均已采用新枚举

#### 版本说明

| 版本 | 日期 | 主要变更 |
|---|---|---|
| v1.0 | 2026-08-10（推断） | 原始四节产品定义（核心定位 / 内容生产规则 / 页面结构 / 自定义能力） |
| v1.1 | 2026-08-11 | 升级为自包含版本：新增数据契约、评分细则、默认值基线、异常态、非功能性需求、UAT、范围外、冲突裁决 8 个章节 |
| v1.1.1 | 2026-08-12 | 矛盾冲突补丁：1 BLOCKER + 7 HIGH + 4 PM-决策项（共 12 处修复） |
| **v1.2** | **2026-08-12** | **对齐《后端集成接口文档 v1.0》：本期范围严格按接口文档 9 个接口 + 固定 4 源 + 4 类型重写。v1.1.1 时期的自定义信息源/标签/评分/检索/历史/已读等能力推迟到 v2。新增决策 5/6/7。** |

---

*AI 日报系统 · PRD v1.2 · 2026-08-12 · 与《后端集成接口文档 v1.0》完全对齐，v1.x 范围以接口文档为权威契约*
