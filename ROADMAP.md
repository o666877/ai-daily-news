# AI 日报系统 · 产品路线图（ROADMAP）

- **版本**：v1.2
- **日期**：2026-08-12
- **作者**：产品组
- **状态**：定稿，对齐《后端集成接口文档 v1.0》
- **关联文档**：《PRD v1.2》《后端集成接口文档 v1.0》
- **产品定位回顾**：高度自定义的私人资讯聚合工具，先个人本地部署使用，成熟后开源至 GitHub。本期路线图围绕"本地单用户 MVP → 开源发布"一条主线推进。
- **v1.2 对齐说明**：本期范围严格按接口文档 v1.0 的 9 个接口（§3）+ 固定 4 源 + 4 类型规划。原 v1.1 时期规划的自定义信息源/标签 CRUD、daily_count/style_mode 配置、评分体系、检索、历史、已读等能力，**接口文档 v1.0 未提供对应接口的，统一推迟到 v2**（详见各阶段范围调整与 §三 v2 候选清单）。

---

## 一、阶段总览

> v1.2 对齐说明：本期阶段重新切分以匹配接口文档 v1.0 的 9 个接口。原 v1.1 的"阶段 2 配置（信息源/标签 CRUD）"和"阶段 3 调度与历史（日历/检索/已读）"涉及的能力本期无接口，对应工作内容推迟到 v2。本期阶段 0-5 聚焦接口文档已覆盖的能力。

| 阶段 | 名称 | 目标产出 | 关键里程碑 |
|---|---|---|---|
| 阶段 0 | 基础架构 | 数据模型落库、采集/加工/展示三层骨架、9 个接口桩 | — |
| 阶段 1 | 今日刊 MVP | `GET /daily/today` + `GET /articles` + `GET /articles/{id}` + `GET /meta` 端到端打通 | — |
| 阶段 2 | 偏好与分享 | `GET/PUT /settings` + `POST /settings/reset` + `POST /share` + 健康检查 | — |
| 阶段 3 | 联调与稳定 | 9 个接口联调通过、错误码全覆盖、限流与鉴权就位 | **里程碑 M1：v1.0 本地版** |
| 阶段 4 | 打磨与开源准备 | 性能、文档、配置外置、合规 | — |
| 阶段 5 | 开源发布 | 仓库公开、README、社区接入路径 | **里程碑 M2：v1.1 开源版** |
| v2 | 自定义能力扩展 | 接口文档扩展后启动：信息源/标签 CRUD、检索、历史、已读、评分、daily_count、style_mode 等 | — |

---

## 二、分阶段定义

### 阶段 0 · 基础架构

- **目标**：搭好采集 / 加工 / 展示三层骨架与数据模型落库，让后续业务逻辑有承载基底；9 个接口桩就位。
- **范围**（v1.2 对齐接口文档 §3 + §4）：
  - 数据库 schema 落地（**Article / DailyIssue / Settings 三张核心表**，含版本化迁移脚本；本期无 Source/Type/DomainType/read_state 独立表，元数据由 `/meta` 接口静态返回或由配置文件驱动）
  - 采集层抽象（X / GitHub / Reddit / 全网聚合 四类固定信息源的最小可运行抓取）
  - 加工层抽象（采集 → LLM 摘要 → 刊期状态机骨架，规则可后续填充）
  - 展示层骨架（前端单页 + Base URL `/api/v1` + 统一响应包裹 + 错误结构 §2.1）
  - 9 个接口桩就位（即使返回 mock 数据也要符合接口文档 §4 数据模型）
- **退出标准 DoD**：
  - [ ] 三张表迁移脚本可在空库上一键跑通
  - [ ] 采集层至少能成功抓取一类源（如 GitHub Trending）并入库
  - [ ] LLM 摘要可空跑（即使 prompt 未优化，也能透传原文段落）
  - [ ] 9 个接口桩全部返回符合接口文档 §4 结构的 JSON（即便是 mock 内容）
  - [ ] `GET /healthz` 返回 `{ status, version, pipeline: { collector, summarizer } }`
- **依赖**：无外部依赖；选型决策（数据库 / 抓取框架 / LLM 提供方）需在本阶段冻结
- **风险**：
  - 选型返工风险（数据库 / LLM 提供方）→ 缓解：阶段 0 第一周做选型 spike，写最小 PoC
  - LLM 提供方不可达或成本超预期 → 缓解：摘要环节预留可替换接口，本地小模型 fallback

### 阶段 1 · 今日刊 MVP

- **目标**：单用户能每天看到一份可读的今日刊，验证"采集 → LLM 摘要 → 刊期 → 浏览"端到端链路。本阶段聚焦**读接口**（4 个）。
- **范围**（v1.2 对齐接口文档 §5.1 / §5.2 / §5.3 / §5.4）：
  - `GET /daily/today` — 今日刊概览（报头日期/版次 + 索引列表 + 数量徽标）
  - `GET /articles` — 列表 + 筛选（type/src 组合）+ 分页
  - `GET /articles/{id}` — 条目详情（完整 Article 字段）
  - `GET /meta` — 元数据（4 源 + 4 类型，**驱动前端筛选 chips 与设置面板**）
  - LLM 摘要生成（lede / summary / body[] / quote / points[]）
  - 刊期状态机（`generating → ready / failed`）
  - 前端今日刊视图 + 资讯详情视图（左侧索引 + 右侧阅读器）
- **退出标准 DoD**：
  - [ ] `curl GET /daily/today` 拿到完整结构（issue + summary + articles[]）
  - [ ] `curl GET /articles?type=agent&src=reddit` 筛选工作正常
  - [ ] `curl GET /articles/{id}` 返回完整 Article
  - [ ] `curl GET /meta` 返回 4 sources + 4 types
  - [ ] 前端筛选 chips 由 `/meta` 驱动，**不硬编码**
  - [ ] 刊期 `generating` (2003) → 骨架屏；`ready` → 正常渲染
  - [ ] 刊期 `failed` (2002) → 与未生成一致
  - [ ] LLM 摘要失败重试 2 次仍失败标记 `failed`
- **依赖**：阶段 0 完成；LLM 提供方可用
- **风险**：
  - LLM 摘要质量不达标 → 缓解：阶段 1 内置 prompt 模板与人工抽检流程
  - 4 源采集稳定性参差（X 反爬激进）→ 缓解：失败静默跳过，不阻塞刊期生成
  - 元数据驱动前端不适应 → 缓解：阶段 1 第一周敲定 `/meta` 返回格式后冻结

### 阶段 2 · 偏好与分享

- **目标**：让用户能配置采集偏好与分享文章，验证"写接口 + 鉴权 + 异步生效"链路。本阶段聚焦**写接口**（4 个）+ 健康检查（1 个）。
- **范围**（v1.2 对齐接口文档 §5.5 / §5.6 / §5.7 / §5.8 / §5.9）：
  - `GET /settings` — 获取用户偏好（需 Bearer token）
  - `PUT /settings` — 全量保存偏好，响应头 `X-Effective-At` 提示生效刊期
  - `POST /settings/reset` — 恢复默认偏好（全开 + 08:00 推送）
  - `POST /share` — 生成分享卡片，返回 `shareId` / `cardUrl` / `articleTitle`
  - `GET /healthz` — 健康检查（联调/监控用）
  - 鉴权机制：读接口匿名可用，写接口需 `Authorization: Bearer <token>`
  - 异步生效：偏好保存后同步给采集调度器，下一期刊期生效
  - 限流：读 120 次/分钟/IP，写 30 次/分钟/用户
- **退出标准 DoD**：
  - [ ] `curl -H "Authorization: Bearer xxx" GET /settings` 返回当前偏好
  - [ ] `PUT /settings` 全量覆盖，重复提交幂等
  - [ ] `PUT /settings` 响应头包含 `X-Effective-At`
  - [ ] `POST /settings/reset` 返回默认偏好（全开 + 08:00）
  - [ ] `POST /share` 返回 cardUrl，文章不存在返回 404/2001
  - [ ] 写接口无 token 返回 401/1003
  - [ ] 限流触发返回 429/1006
  - [ ] `GET /healthz` 返回 pipeline 状态
- **依赖**：阶段 1 完成；鉴权机制选型冻结
- **风险**：
  - 偏好保存与采集调度的异步一致性 → 缓解：明确"下一期生效"语义，前端用 `X-Effective-At` 提示
  - 分享卡片的存储与隐私 → 缓解：卡片 id 用短随机串，不暴露 articleId 原值
  - 鉴权机制对本地单用户的繁琐度 → 缓解：兼容"无 token 视为本机默认用户"

### 阶段 3 · 联调与稳定

- **目标**：9 个接口联调通过，错误码全覆盖，限流与鉴权就位。本阶段末交付 **v1.0 本地版**。
- **范围**（v1.2 对齐接口文档 §9 联调自测清单）：
  - 9 个接口的端到端联调（接口文档 §9 自测清单全部跑通）
  - 错误码全覆盖（1001/1002/1003/1004/1005/1006/2001/2002/2003/9001/9002）
  - 限流与鉴权在生产路径上验证
  - 前端 9 个交互点全部对应到接口（接口文档 §6 接入对照表）
  - 错误响应仅含 `code` / `message` / `requestId`，脱敏审计
- **退出标准 DoD（v1.0 本地版验收）**：
  - [ ] 接口文档 §9 联调自测清单全部通过
  - [ ] PRD v1.2 第十章 UAT 模块 1-9 全部勾选
  - [ ] 错误响应结构统一，无内部栈/SQL/密钥泄露
  - [ ] `X-Request-Id` 头透传工作正常
  - [ ] 时区统一 Asia/Shanghai，时间格式 ISO 8601
  - [ ] 元数据 100% 由 `/meta` 驱动，前端无硬编码
- **依赖**：阶段 2 完成
- **风险**：
  - 错误码边界场景遗漏 → 缓解：以接口文档 §2.2 + §9 为验收基线，逐条对照
  - 鉴权与限流的误伤 → 缓解：本地单用户场景下放宽写接口限流阈值
- **里程碑**：**M1 — v1.0 本地版发布**（仅本人使用，不公开）

### 阶段 4 · 打磨与开源准备

- **目标**：把"能跑"的产品打磨到"别人能跑"，为开源发布做合规、文档、性能、配置外置等准备。
- **范围**：
  - 性能优化（`GET /daily/today` P95 ≤ 500ms、`GET /articles` P95 ≤ 300ms、`GET /articles/{id}` P95 ≤ 200ms）
  - 配置项外置（默认源、默认类型、LLM prompt 模板、抓取限速、推送时间等进配置文件，不硬编码）
  - 日志体系（结构化日志：每日采集条数 → LLM 摘要成功率 → 刊期生成耗时；抓取失败；token 消耗）
  - 错误信息脱敏（不泄露内部栈 / SQL / 密钥）
  - 数据库迁移脚本版本化与回滚验证
  - README 初稿（本地启动步骤、9 个接口调用示例、配置项说明、日志位置）
  - 关键算法单元测试覆盖（采集调度、LLM 摘要 prompt、刊期状态机）
  - 合规自查（robots / ToS、原文不缓存全文、抓取限速）
  - 鉴权机制文档化（兼容"无 token 视为本机默认用户"）
- **退出标准 DoD**：
  - [ ] 性能指标全部达标
  - [ ] 所有配置项可在配置文件中修改
  - [ ] 日志可读、可观测，含每日生成质量数据
  - [ ] 错误响应脱敏通过人工审计
  - [ ] 一份新人能照着跑起来的 README
  - [ ] 关键算法单测覆盖 ≥ 80%
  - [ ] 合规自查清单全部勾选
- **依赖**：阶段 3 完成
- **风险**：
  - 合规风险（目标平台 ToS 限制抓取）→ 缓解：限速 + 优先走官方 API；预留被源站封禁的兜底
  - 性能优化挤占开源时间 → 缓解：达标即可，不追求极致
  - 文档与代码脱节 → 缓解：README 与配置项注释作为本阶段硬性 DoD

### 阶段 5 · 开源发布

- **目标**：仓库公开，社区可拉取、可部署、可贡献。本阶段末交付 **v1.1 开源版**。
- **范围**：
  - 仓库整理（LICENSE、CONTRIBUTING、CODE_OF_CONDUCT、Issue/PR 模板）
  - GitHub Release v1.1（含 CHANGELOG、一键启动脚本、docker-compose 可选）
  - 默认 4 源 + 4 类型的"出厂值"沉淀到配置文件
  - 文档站（或 docs/ 目录）：快速开始、9 个接口集成指南、自定义指南（v2 预告）、FAQ、架构图
  - 社区接入路径（Issue 反馈、讨论区、贡献者指南）
  - 鉴权兼容（同阶段 4）
- **退出标准 DoD（v1.1 开源版验收）**：
  - [ ] 一个全新 clone 的用户按 README 30 分钟内能跑起来
  - [ ] LICENSE / CONTRIBUTING / CODE_OF_CONDUCT 齐全
  - [ ] Issue / PR 模板可用
  - [ ] v1.1 Release Notes 与 CHANGELOG 对齐 PRD v1.2 + 接口文档 v1.0
  - [ ] 默认 4 源合法可抓（不内置明显违反 ToS 的源）
  - [ ] 合规与隐私声明（数据归属、抓取边界、原文不缓存）出现在 README 显眼位置
- **依赖**：阶段 4 完成；开源合规审阅通过
- **风险**：
  - 内置信息源导致版权/抓取争议 → 缓解：内置仅放明显开放、ToS 允许的源；其他作为"示例配置"注释
  - 社区 issue 涌入压垮维护节奏 → 缓解：明确 v1.x 维护节奏（单人维护，响应窗口 7 天）
- **里程碑**：**M2 — v1.1 开源版发布**

---

## 三、v2 探索阶段（反馈驱动）

> v1.2 对齐说明：v2 候选清单分为两部分——A 部分为本期因接口文档 v1.0 未覆盖而推迟的能力（接口扩展后即可启动），B 部分为长期不做的能力（与产品哲学/定位冲突）。优先级由开源后的社区反馈决定。

### A. 本期推迟的能力（接口文档扩展后启动）

| 候选 | 接口文档待扩展 | 当前评估 |
|---|---|---|
| 历史日历 + 历史日报访问 | `GET /daily/{date}` / `GET /daily/calendar` | 阅读回看是核心场景，建议接口文档 v1.1 优先扩展 |
| 关键词检索 | `GET /search` | 单用户小数据量即可满足，实现成本低 |
| 已读跟踪（自动标记 + 视觉淡化） | Article 加 `is_read` 字段 + `/read-state` 接口 | 与"精简阅读"定位强相关，建议优先 |
| 自定义信息源管理 | CRUD `/sources` + `kind`/`credibility`/`tier` 字段 | 高价值，但需评估采集层扩展成本 |
| 自定义领域标签管理 | CRUD `/types` + `weight`/`specificity`/`order` 字段 | 高价值，需配套评分体系 |
| 综合评分体系 | Article 加 `score` 字段 | 配合标签管理一起落地 |
| `daily_count` 自定义 | Settings 加 `daily_count` 字段 | 实现成本低 |
| `style_mode` 三档 | Settings 加 `style_mode` 字段 | 实现成本低，但需 LLM prompt 配合 |
| 当日核心结论 | DailyIssue 加 `conclusions` 字段 | LLM 成本翻倍，需评估 |
| 按领域分组快照 | DailyIssue 加 `sections` 字段 | 与核心结论一起落地 |
| `src_filter` 永久筛选状态 | Settings 加 `src_filter` 字段 | 实现成本低 |
| 手动「立即生成」 | `POST /generate/run` | 联调/运维刚需，建议优先 |
| 配置变更联动当日重算 | 重算接口 + 即时生效语义 | 与"下一期生效"语义冲突，需重新设计 |

### B. 长期不做的能力（与产品哲学/定位冲突）

| 候选 | 触发条件 | 当前评估 |
|---|---|---|
| 多用户 / 账号体系 | 社区出现明确多租户部署诉求 | 与本地单用户定位冲突，谨慎 |
| 移动端 / 响应式适配 | 移动端访问占比 > 20% | 网页端响应式优先，原生 App 暂不评估 |
| 推送通知（邮件 / IM） | 用户反馈主动触达是刚需 | 需引入外部服务，与本地定位有张力 |
| 收藏 / 标注 / 笔记 | 多用户在 issue 中提出 | 与"精简"定位有张力，按反馈再判 |
| 自动新增领域标签 | 自动归类准确率 ≥ 95% 且用户接受 | 风险高，需大量样本验证 |
| 多语言界面 | 社区贡献者提交 i18n PR | 接受贡献驱动 |
| 跨设备同步 | 云端版本进入规划 | 需先解决云端部署 |
| 阅读统计仪表盘 | 数据沉淀足够且用户主动要求 | 与核心价值弱相关 |
| RSS / Atom 输出 | 用户明确希望接入第三方阅读器 | 实现成本低，可优先 |
| LLM 提供方可插拔 | 用户反馈默认 provider 不可用 | 强需求，建议阶段 4 即做接口预留 |
| 全文检索引擎接入 | 检索性能在历史数据增长后下滑 | 数据量未达阈值前不必要 |
| 算法个性化推荐 | 永不做 | **与产品哲学冲突**（摆脱算法投喂是核心定位） |

**永不做**（与产品哲学冲突）：算法个性化推荐。

---

## 四、跨阶段关注项

> 这些关注项不归属任何单一阶段，需要在每个阶段持续投入。

| 关注项 | 要求 | 落地形式 |
|---|---|---|
| **摘要质量** | 每个阶段都需抽检今日刊 LLM 摘要（lede / summary / body / quote / points）的可读性、准确性、风格一致性 | 阶段 1 起，每日生成后人工抽检；阶段 4 起加入日志埋点便于回溯 |
| **合规** | 抓取限速、遵守 robots / ToS、原文不缓存全文、错误信息脱敏 | 阶段 0 选型时即纳入考量；阶段 4 做合规自查清单 |
| **隐私** | 用户偏好属私人数据不可外发；开源版明确数据归属 | 阶段 5 README 显眼位置声明 |
| **接口契约稳定性** | 9 个接口的字段/枚举/错误码一旦发布不可破坏性变更；新增字段需向后兼容 | 阶段 1 起冻结接口；阶段 5 起作为开源版的 semver 承诺 |
| **元数据驱动** | 筛选 chips 与设置面板开关 100% 由 `/meta` 驱动，前端不硬编码 | 阶段 1 起作为代码审查必查项 |
| **文档同步** | PRD / 接口文档 / ROADMAP / README 任何代码变更后同步 | 每次合并 PR 前检查文档差异；阶段 4 起作为合并门槛 |
| **可观测性** | 刊期生成全过程可追溯；采集/LLM 失败可定位；`X-Request-Id` 透传 | 阶段 1 起记录最小日志；阶段 4 完善结构化日志 |
| **可测试性** | 关键算法有单测；9 个接口端到端可回归 | 阶段 1 起为 LLM 摘要 prompt / 刊期状态机补单测；阶段 3 起补 UAT 自动化 |

---

## 五、决策记录

### DR-01 `style_mode` 默认值 = balanced

- **决策**：默认 `balanced`（非接口文档中的 `lively`）。
- **依据**：PRD 第二节 §5 原文「默认平衡档」。接口文档 `lively` 属笔误。
- **影响阶段**：阶段 1（首启体验）、阶段 4（开源出厂值）。
- **详见**：PRD v1.1 第八章决策 1。

### DR-02 已读状态独立于 `Settings`，拆独立 `read_state` 表 + 模块 C 专用接口

- **决策**：`read_state` 独立存储，不放进 `Settings.read_ids`。
- **依据**：避免 `PUT /settings` 全量覆盖请求体无限膨胀；配置 vs 状态语义分离；接口文档 `PUT /settings` 本身已排除 `read_ids`，存在一致性缺陷。
- **影响阶段**：阶段 0（数据模型）、阶段 3（已读接口与视觉淡化）。
- **详见**：PRD v1.1 第八章决策 2。

### DR-03 检索范围 = 标题 + 摘要 + 导语 + 解读正文

- **决策**：采纳接口文档的更全面版本，扩展 PRD 原文「标题+解读内容」。
- **依据**：覆盖 Article 模型所有用户可见文本字段，召回率更高，性能代价可忽略。
- **影响阶段**：阶段 3（检索实现）。
- **详见**：PRD v1.1 第八章决策 3。

### DR-04 `daily_count` 默认值 = 10

- **决策**：默认 10（5/8/10/12 中位偏上）。
- **依据**：贴合「单次深度阅读约 30 分钟」定位；认知成本最低。
- **影响阶段**：阶段 1（首启基线）、阶段 4（开源出厂值）。
- **详见**：PRD v1.1 第八章决策 4。

### DR-05 数据库选型推迟到阶段 0 选型 spike

- **决策**：不预先指定数据库（SQLite / PostgreSQL / 其他），阶段 0 第一周做选型 spike 后冻结。
- **依据**：本地单用户与开源两种部署形态对数据库的运维诉求差异大（SQLite 零依赖 vs PostgreSQL 工业级），需在 spike 中量化权衡。
- **影响阶段**：阶段 0。
- **回滚条件**：spike 显示候选方案均不满足性能/可维护性要求时，重新评估。

### DR-06 LLM 提供方抽象成可替换接口

- **决策**：摘要/结论生成的 LLM 调用层抽象为接口，默认 provider 可替换。
- **依据**：开源后面向不同地区用户，默认 provider 可能不可达（网络/合规/成本），必须可替换；阶段 4 起作为开源准备的硬性要求，阶段 1 起即做接口隔离。
- **影响阶段**：阶段 1（接口隔离）、阶段 4（开源准备）、v2（可插拔候选）。

### DR-07 历史日报不可变，配置变更仅作用于当日及未生成日报

- **决策**：任何配置变更（`daily_count` / `style_mode` / 领域权重 / 信息源启停 / 标签删除）都**不回溯改写已生成的历史日报**。
- **依据**：保证用户的"阅读历史"是稳定快照，避免"昨天看过的内容今天消失了"的不可逆体验；降低重算成本。
- **影响阶段**：阶段 2（配置联动）、阶段 3（UAT 必查）。
- **唯一例外**：阶段 2 的领域权重变更允许重算"当日及未生成日报"，已生成历史仍不变。

### DR-08 开源版仅内置"明显开放、ToS 允许"的信息源

- **决策**：开源仓库出厂的默认信息源仅放 RSS / 官方 API 明确允许抓取的源；其他作为"示例配置"注释存在，由部署者自行启用。
- **依据**：避免开源即诱导违规；降低维护者合规风险。
- **影响阶段**：阶段 5。

### DR-09 ROADMAP 与 PRD 强对齐，不再维护独立的 `PRD-补充.md`

- **决策**：`PRD-补充.md` 的内容已并入 PRD（第五至十一章），不再独立维护，避免多源真相。
- **依据**：单一真相源原则；上一轮产出的补充章节已经被 PRD 完整吸收并重新审视。
- **影响阶段**：立即生效。

### DR-10（v1.2 新增）本期范围以接口文档 v1.0 为权威，PRD v1.1.1 自定义能力推迟到 v2

- **决策**：v1.x 范围严格对齐接口文档 v1.0 的 9 个接口（§3）+ 固定 4 源 + 4 类型。PRD v1.1.1 时期规划但接口文档未提供的能力（自定义信息源/标签 CRUD、daily_count/style_mode 配置、评分体系、检索、历史日历、已读跟踪等），统一推迟到 v2 backlog。
- **依据**：接口文档是工程可开工的权威契约；MVP 优先验证"采集 → LLM 摘要 → 刊期 → 浏览 → 分享 → 偏好"端到端链路；避免范围蔓延。
- **影响阶段**：本期阶段 0-3 重新切分（原阶段 2 配置 + 阶段 3 调度与历史的能力推迟到 v2）；阶段 4-5 范围对应收窄。
- **详见**：PRD v1.2 §十二决策 5。

### DR-11（v1.2 新增）字段命名以接口文档 camelCase 为准

- **决策**：本期字段命名统一采用接口文档 §4 实际定义的 camelCase（`issueId` / `sourceUrl` / `publishedAt` / `readingMinutes` / `generatedAt` / `articleCount` / `filtersApplied` / `shortName` / `dailyPush` / `updatedAt`），不采用 §7 描述的"snake_case"。
- **依据**：接口文档 §4 字段定义是工程直接消费的契约，优先级高于 §7 描述性章节。§7 的"snake_case"应理解为枚举值的多单词分隔（如 `self_improve` / `open_source`），与字段命名无关。
- **影响阶段**：阶段 0（数据模型）、阶段 1（接口桩）、阶段 2（写接口）。
- **详见**：PRD v1.2 §十二决策 6。

### DR-12（v1.2 新增）信息源 `blogs` → `reddit` 更名生效

- **决策**：本期固定 4 源为 `x` / `github` / `reddit` / `web`，不再保留 `blogs` 枚举。历史数据中 `blogs` 统一映射为 `reddit`。
- **依据**：接口文档 §7 明确"2026-08-12 起，原「博客」源已更名"，前端原型已同步。
- **影响阶段**：阶段 0（数据迁移）、阶段 1（`/meta` 返回值）。
- **详见**：PRD v1.2 §十二决策 7。

### DR-13（v1.2 新增）鉴权策略：读匿名 + 写 Bearer token

- **决策**：读接口（`GET /daily/today` / `GET /articles` / `GET /articles/{id}` / `GET /meta` / `GET /healthz`）匿名可用；写接口（`GET /settings` / `PUT /settings` / `POST /settings/reset` / `POST /share`）需 `Authorization: Bearer <token>`。
- **依据**：接口文档 §2 明确"读接口匿名可用；写接口需 Bearer token"。本地单用户部署兼容"无 token 视为本机默认用户"以降低繁琐度。
- **影响阶段**：阶段 2（写接口）、阶段 4（开源鉴权文档化）。
- **回滚条件**：开源后出现多用户部署诉求时，重新评估读接口鉴权策略。

---

## 六、关键里程碑

### 里程碑 M1 — v1.0 本地版（阶段 3 末）

- **定义**：单用户在本地部署可日常使用，接口文档 v1.0 的 9 个接口全部联调通过，PRD v1.2 第十章 UAT 全部勾选。
- **不上开源**：仅供作者本人验证产品定位与 LLM 摘要质量。
- **下一里程碑**：M2。

### 里程碑 M2 — v1.1 开源版（阶段 5 末）

- **定义**：GitHub 仓库公开，第三方可 30 分钟内按 README 跑起来；PRD v1.2 + 接口文档 v1.0 + ROADMAP v1.2 三份文档对齐发布。
- **进入 v2 探索阶段**：依据社区反馈驱动 v2 候选项优先级，优先扩展接口文档（历史访问、检索、已读、信息源/标签 CRUD 等）。

---

*AI 日报系统 · ROADMAP v1.2 · 2026-08-12 · 与 PRD v1.2、接口文档 v1.0 完全对齐；本期范围以接口文档 9 个接口为权威*
